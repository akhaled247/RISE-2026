# SpecRLBench + SafePO + RISE-RND
#
# SafePO train/eval path does NOT need stable-baselines3.
# ``utils.env_utils.make_env`` is SB3-free.
#
# Do NOT ``pip install -r`` this file blindly with git safepo — PyPI safety-gymnasium
# pins gymnasium==0.28.1 and conflicts with the SpecRL fork (gymnasium>=0.29.1).
#
# Recommended install (from RISE-2026, venv active):
#
#   cd SpecRLBench
#   pip install -e specbench/envs/zones/safety-gymnasium
#   pip install -e ../RISE-RND
#   # SafePO: use SpecRLBenchMods fork (not PKU main):
#   #   git clone -b SpecRLBenchMods \
#   #       https://github.com/akhaled247/Safe-Policy-Optimization.git \
#   #       ../Safe-Policy-Optimization
#   #   touch ../Safe-Policy-Optimization/safepo/single_agent/__init__.py
#   pip install -e ../Safe-Policy-Optimization --no-deps
#   pip install "torch>=2.0" "numpy<2" "pyyaml>=6.0" "tensorboard>=2.14" \
#       "tqdm>=4.65" seaborn pandas joblib "moviepy>=1.0.3" "rich>=13.3.0" \
#       scipy "wandb>=0.13.0"
#
# Smoke train — pass ALL flags on ONE line (or end lines with \). Otherwise
# the shell treats ``--device`` as a command → ``--device: command not found``.
#
#   python train/ppo_train_env.py --task PointLTL4MASAR1WC-v0 --seed 0 \
#       --total-steps 40000 --num-envs 8 --steps-per-epoch 16384 --device cpu \
#       --parallel True
#
# RND-PPO (stock SafePO PPO + RISE-RND wrapper):
#
#   python train/ppo_rnd_train_env.py --task PointLTL4MASAR1-v0 --seed 0 \
#       --total-steps 40000 --num-envs 8 --steps-per-epoch 16384 --device cpu \
#       --sweep S2
#
# --parallel True (default): SafetyAsyncVectorEnv when num-envs>1.
# --parallel False: SyncVectorSafetyEnv (serial; debug only).
#
# Legacy SB3 .zip checkpoints are unsupported; use eval_safepo_env.py for SafePO runs.

# torch>=2.7 dropped LRScheduler verbose=; SpecRLBench patches LinearLR
# via backends/safepo/torch_compat.py so SafePO stock algos still run.
torch>=2.0
gymnasium>=0.29.1,<1.0
numpy<2
pyyaml>=6.0
tensorboard>=2.14
tqdm>=4.65
joblib
moviepy>=1.0.3
rich>=13.3.0
scipy
wandb>=0.13.0
seaborn
pandas
